{
  "private": true,
  "name": "@wyrecc/env",
  "version": "0.1.0",
  "license": "MIT",
  "main": "dist/index.js",
  "files": [
    "*.ts",
    "dist"
  ],
  "scripts": {
    "build": "yarn clean && tsc",
    "clean": "rimraf node_modules dist",
    "dev": "tsc --watch",
    "lint:es": "TIMING=1 eslint \"*.ts\"",
    "lint:ts": "tsc --pretty --noEmit"
  },
  "types": "dist/index.d.ts",
  "dependencies": {
    "zod": "^3.20.2"
  },
  "devDependencies": {
    "@wyrecc/config": "^0.1.0",
    "@wyrecc/tsconfig": "^0.1.0",
    "dotenv": "^16.0.3"
  }
}
