{
  "private": true,
  "name": "@wyrecc/maplerad",
  "version": "0.1.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist/**",
    "package.json"
  ],
  "scripts": {
    "build": "tsc",
    "lint:ts": "tsc --pretty --noEmit",
    "clean": "rimraf node_modules dist",
    "lint:es": "TIMING=1 eslint src"
  },
  "dependencies": {
    "ajv": "^8.12.0",
    "got": "^12.6.0",
    "json-schema": "^0.4.0"
  },
  "devDependencies": {
    "@types/json-schema": "^7.0.11",
    "@wyrecc/config": "^0.1.0",
    "@wyrecc/tsconfig": "^0.1.0"
  }
}
